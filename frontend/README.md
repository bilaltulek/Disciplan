# Disciplan frontend

The React/Vite client is part of the root Disciplan application. Use the root
[README](../README.md) for setup, architecture, API, testing, and deployment.

Frontend-only commands:

```bash
npm run dev
npm run typecheck
npm run test
npm run lint
npm run build
```
